{% block body %}
    <style>
        body {
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            color: white;
            font-family: 'Orbitron', sans-serif;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            text-align: center;
        }

        h1 {
            font-size: 2.5rem;
            text-shadow: 0 0 10px cyan;
            margin-bottom: 20px;
        }

        table {
            width: 80%;
            border-collapse: collapse;
            margin-top: 20px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
        }

        th, td {
            padding: 12px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }

        th {
            background: rgba(255, 255, 255, 0.2);
            text-transform: uppercase;
        }

        tr:hover {
            background: rgba(255, 255, 255, 0.3);
        }

        a {
            color: cyan;
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s;
        }

        a:hover {
            color: #ff0099;
        }

        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 24px;
            font-size: 1.2rem;
            font-weight: bold;
            background: linear-gradient(90deg, #ff0099, #493240);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
            text-decoration: none;
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
        }

        .btn:hover {
            background: linear-gradient(90deg, #ff0099, #6a0572);
            transform: scale(1.05);
        }
    </style>

    <h1>🎨 Liste des Artistes 🎨</h1>

    {% if artists is not empty %}
        <table>
            <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Image</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            {% for artist in artists %}
                <tr>
                    <td>{{ artist.id }}</td>
                    <td>{{ artist.name }}</td>
                    <td>{{ artist.description }}</td>
                    <td>
                        {% if artist.imagePath %}
                            <img src="{{ asset(artist.imagePath) }}" alt="{{ artist.name }}" width="50">
                        {% else %}
                            ❌
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ path('app_artist_by_id', { id: artist.id }) }}">👀 Voir</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>Aucun artiste trouvé.</p>
    {% endif %}

    <a href="{{ path('app_home') }}" class="btn">Retour à l'accueil</a>
{% endblock %}
